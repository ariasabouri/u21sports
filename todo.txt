- Contact form
- Cookie consent modal
- Impressum
- Footer menu
- Implement fontawesome icons
x - Abstract all UI elements that are reusable into own components
- Migrate all components to the composition API using the setup syntax
- When break point at prices and other parts is reached, center the elements of the next line
x - we moved the highlight section into an own component, remove all remaining CSS fragments in other components for the highlight section from them
- check naming convention for all components, variables and classes